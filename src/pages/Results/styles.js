import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  root: {
    flexGrow: 1,
    marginTop: "1rem"
  },
  section: {
    paddingTop: "1rem"
  },
  query: {
    "text-transform": "capitalize"
  }
});
